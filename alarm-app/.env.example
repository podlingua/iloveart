# Supabase project credentials. Required for alarms to be created, persisted,
# and synced across devices. Create a project at https://supabase.com/dashboard,
# then run the migration in supabase/migrations/0001_init.sql against it.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
